{
  "scripts": {
    "dev": "tsnd --respawn --transpile-only ./src/index.ts",
    "tsc-build": "tsc",
    "tsc-watch": "tsc -w",
    "tsc-check": "tsc --noEmit"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.4"
  }
}